function login() {
    // Perform login logic (you may need to check credentials and redirect to home.html)
    // For simplicity, let's just redirect to home.html
    window.location.href = "home.html";
}
